<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'info', user: @user %>
      <%= render 'new', book: @book %>
    </div>
    
    <div class="col-md-8">
      <h1>Book detail</h1>
      <table class="table table-striped">
        <tbody>
          <tr>
            <td>
              <%= image_tag @book.user.get_profile_image(100,100) %>
              <p><%= link_to @book.user.name, user_path(@book.user.id) %></p>
            </td>
            <td><p><%= link_to @book.title, book_path(@book.id) %></p></td>
            <td><%= @book.body %></td>
            <td>
              <% if @book.user.id == current_user.id %>
              <span class="link">
                <%= link_to edit_book_path(@book.id) do%>
                  <button type="button" class="btn btn-success">Edit</button>
                <% end %>
              </span>
              <span class="link">
                <%= link_to book_path(@book.id), method: :delete, data: { confirm: "本当に消しますか？" } do %>
                  <button type="button" class="btn btn-danger">Destory</button>
                <% end %>
              </span>
              <% end %>
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</div>